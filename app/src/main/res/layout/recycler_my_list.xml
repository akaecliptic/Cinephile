<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="30dp">

    <TextView
        android:id="@+id/myListTVYear"
        android:layout_width="wrap_content"
        android:layout_height="25dp"
        android:layout_centerVertical="true"
        android:layout_marginStart="7dp"
        android:layout_marginTop="0dp"
        android:layout_toEndOf="@+id/checkBoxSeen"
        android:text="@string/year"
        android:textColor="#fff"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/myListTVTitle"
        android:layout_width="200dp"
        android:layout_height="25dp"
        android:layout_centerVertical="true"
        android:layout_marginStart="7dp"
        android:layout_marginTop="0dp"
        android:layout_toEndOf="@+id/myListTVYear"
        android:ellipsize="marquee"
        android:focusable="true"
        android:marqueeRepeatLimit="marquee_forever"

        android:scrollHorizontally="true"
        android:singleLine="true"
        android:text="@string/title"
        android:textColor="#fff"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/myListTVRating"
        android:layout_width="wrap_content"
        android:layout_height="25dp"
        android:layout_centerVertical="true"
        android:layout_marginStart="7dp"
        android:layout_marginTop="0dp"
        android:layout_toEndOf="@+id/myListTVTitle"
        android:maxLength="2"
        android:text="@string/rating"
        android:textColor="#fff"
        android:textSize="20sp" />

    <CheckBox
        android:id="@+id/checkBoxSeen"
        android:layout_width="wrap_content"
        android:layout_height="25dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="7dp"
        android:layout_marginTop="0dp"
        android:textColorLink="#000" />

</RelativeLayout>